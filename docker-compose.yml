services:
  agentrouter:
    build:
      context: .
      dockerfile: Dockerfile
    image: agentrouter:local
    ports:
      # Publish only Nginx. Internal web/gateway listeners stay inside the container.
      - "3458:8080"
    volumes:
      - ar-data:/data
    restart: unless-stopped

volumes:
  ar-data:
