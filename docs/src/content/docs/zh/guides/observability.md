---
title: 开启日志与观测
pageTitle: 开启日志与观测
eyebrow: 快速开始
lead: 首次接入 AgentRouter 后的验证步骤：在设置中打开请求日志与 Agent 观测，发一条请求，然后在请求日志和观测面板里确认链路生效。适用于第一次部署时的链路联调。
---

## 开启日志与观测

到 **设置 → 日志与观测**：

1. 打开 **请求日志**。
2. 打开 **Agent 观测**。

## 查看观测面板

观测面板用于查看 Agent 的执行链路和性能表现：每个步骤何时发生、调用了哪个工具、工具获得了什么结果、耗时多久、是否出错，以及后续步骤如何继续。

它可以帮助定位 Agent 卡住、工具结果异常、某一步耗时过长，或上下文流转不符合预期等问题。请求日志提供单条模型请求的请求体、响应体和错误信息。

## 请求日志

请求日志记录经过 AgentRouter 的模型请求明细，包括请求时间、请求 ID、客户端、路径、请求模型、最终命中的供应商和模型、凭据、状态码、耗时、token、成本估算、请求体、响应体和错误信息。

日志页支持按状态、供应商、模型、凭据、请求 ID、模型名、请求体或响应体筛选。单条记录会展示请求与响应的主要字段，包含 `request model`、`resolved provider`、`resolved model`、状态码、响应体、错误信息、耗时、token 和成本估算。

日志和观测共用请求数据。到 **设置 → 日志与观测 → 日志保存天数** 选择保存期限，支持 1、3、7、14、30、90、365 天，默认 1 天。记录超过所选天数后自动清理，按完整 24 小时计算，不在零点统一清空。日志写入服务运行期间每分钟检查一次，同时删除过期记录的关联轨迹和无引用正文文件。

各开关和面板能力的完整说明见 [日志与可观测性配置参考](../../configuration/observability/)。
