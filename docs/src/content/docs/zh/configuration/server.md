---
title: 服务配置
pageTitle: 服务配置
eyebrow: 详细配置
lead: 配置 AgentRouter 网关监听地址、端口，以及通过代理模式进行 MITM 劫持并代理到 AgentRouter 的能力。
---

## 先区分管理地址和网关地址

**服务配置** 中的 Host / Port 配置模型网关；浏览器管理页面由另一套地址和端口负责：

| 运行方式 | 管理入口 | 模型网关 |
| --- | --- | --- |
| 桌面应用 | 应用窗口 | 默认 `http://127.0.0.1:3466` |
| npm CLI | 默认 `http://127.0.0.1:3458` | 默认 `http://127.0.0.1:3466` |
| Docker | 默认公开入口 `http://127.0.0.1:3458` | 由 Nginx 合并到同一公开入口 |

CLI 的 `--host` / `--port` 配置管理服务；本页字段配置模型网关。Docker 的内部管理和网关端口不应单独发布，详见 [Docker 部署](../../guides/docker/)。

## 主字段

| 字段 | 代表的能力 |
| --- | --- |
| Host | AgentRouter 网关监听的主机地址。常见值是 `127.0.0.1` 或 `0.0.0.0`。 |
| Port | AgentRouter 网关监听端口。客户端需要把 API Base URL 指向这个端口。 |

Host 使用 `127.0.0.1` 时仅本机可访问；`0.0.0.0` 会监听所有 IPv4 网卡。只有在确实需要局域网或远程访问时才使用通配地址，并同时配置 AgentRouter 客户端 API Key、防火墙 / 私网和 TLS 反向代理。

管理 Token、AgentRouter 客户端 API Key 和上游供应商凭据彼此独立。客户端访问网关时使用 **API 密钥** 页面创建的 AgentRouter Key，不要直接暴露上游凭据。

## 启动和验证

1. 至少添加一个供应商和模型。
2. 在 **API 密钥** 页面创建客户端 Key。
3. 点击 **启动** 或 **重启**。
4. 确认状态显示运行中，并请求网关的 `/health`。
5. 发出最小模型请求，再到请求日志核对最终供应商 / 模型。

管理 UI 可访问不代表模型网关已运行。Docker 在网关未启动时会让 `/health` 返回 `502`；桌面版 / CLI 也可能在没有可用模型时只保留管理服务。

## 代理模式

代理模式是本地代理能力。开启后，客户端可以把 HTTP/HTTPS 流量交给 AgentRouter；AgentRouter 会通过 MITM 劫持识别和解密 HTTPS 请求，并把可处理的模型请求代理到 AgentRouter 网关链路。

| 字段 | 代表的能力 |
| --- | --- |
| 代理模式 | 启用本地代理能力。开启后 AgentRouter 可以作为 HTTP/HTTPS 代理接收客户端流量，并通过 MITM 劫持把模型请求代理到 AgentRouter。 |
| 系统代理 | 将系统代理指向 AgentRouter。适合让支持系统代理的应用自动经过 AgentRouter。 |
| 捕获网络 | 保存代理模式下经过 AgentRouter 的网络请求，用于“网络”页面查看请求和响应详情。 |
| CA 证书 | 当前代理 CA 证书的信任状态。 |
| 安装 CA | 将 AgentRouter 代理 CA 安装到系统或当前用户信任存储中。不同系统的安装方式不同。 |
| 检查信任 | 重新检测代理 CA 是否已被系统信任。 |
| 代理状态 | 显示代理服务当前是否运行。 |
| 重启代理 | 代理模式开启时，重新启动代理服务。 |

代理模式需要操作本机网络和证书信任，主要面向桌面环境。容器部署通常应把客户端直接指向 AgentRouter 的 Nginx 网关入口，不建议依赖容器修改宿主机系统代理或安装宿主机 CA。
