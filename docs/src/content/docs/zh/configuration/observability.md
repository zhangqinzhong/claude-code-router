---
title: 日志与可观测性
pageTitle: 日志与可观测性
eyebrow: 详细配置
lead: 记录模型请求、查看 Agent 执行链路，并配置日志保存期限与正文采样。
---

## 开启方式

到 **设置 → 日志与观测**：

1. 打开 **请求日志**，记录经过 AgentRouter 的请求明细。
2. 打开 **Agent 观测**，让观测面板展示 Agent 的执行链路。

## 查看观测面板

观测面板用于查看 Agent 的执行链路和性能表现：每个步骤何时发生、调用了哪个工具、工具获得了什么结果、耗时多久、是否出错，以及后续步骤如何继续。

它可以帮助定位 Agent 卡住、工具结果异常、某一步耗时过长，或上下文流转不符合预期等问题。请求日志提供单条模型请求的请求体、响应体和错误信息。

## 请求日志

请求日志记录经过 AgentRouter 的模型请求明细，包括请求时间、请求 ID、客户端、路径、请求模型、最终命中的供应商和模型、凭据、状态码、是否成功、耗时、token、成本估算，以及请求头、请求体、响应头、响应体和错误信息。

日志页支持按状态、供应商、模型、凭据、请求 ID、模型名、请求体或响应体筛选。单条记录会展示请求与响应的主要字段，包含 `request model`、`resolved provider`、`resolved model`、状态码、响应体、错误信息、耗时、token 和成本估算。

日志和观测共用请求数据。到 **设置 → 日志与观测 → 日志保存天数** 选择保存期限，支持 1、3、7、14、30、90、365 天，默认 1 天。记录超过所选天数后自动清理，按完整 24 小时计算，不在零点统一清空。日志写入服务运行期间每分钟检查一次，同时删除过期记录的关联轨迹和无引用正文文件。

## 请求体捕获与采样

请求日志的请求体/响应体记录受三个底层配置项控制（默认值适合大多数场景）：

| 配置项 | 默认值 | 说明 |
| --- | --- | --- |
| `requestLogBodyCapture` | `all` | 控制是否记录请求体和响应体：`all` 记录全部，`errors` 只记录失败请求的 body，`none` 不记录任何 body。 |
| `requestLogMaxBodyBytes` | `52428800`（50 MiB） | 单个请求体或响应体的内存捕获与预览预算。基于文件的 raw trace body 可以超过该限制；完整 body 会保存到 sidecar 存储，并在日志页按需加载。 |
| `requestLogSuccessSampleRate` | `1` | 成功请求的采样率，取 `0` 到 `1` 之间，`1` 表示全部记录，`0.1` 表示记录约十分之一。 |


## 时间与速率

| 指标 | 计算方式 |
| --- | --- |
| 首 Token | 网关开始处理请求，到收到第一个有效内容增量的时间。非流式请求没有这一指标。 |
| 输出速率 | 流式请求的 `(输出 Token − 1) ÷ (总耗时 − 首 Token 延迟)`，时间单位为秒。数据集中返回、短回复和网络缓冲可能放大结果，不能据此推断模型内部生成速度。 |
| 平均吞吐率 | `输出 Token ÷ 请求总耗时（秒）`，包含等待时间，支持流式与非流式请求。 |
| 持续时间 | 该请求记录的总耗时。 |

Token 用量来自供应商响应；输出 Token 可能包含思考 Token。缺少有效数据时显示 `-`。

## 磁盘占用

保存天数仅控制请求日志及由它生成的观测数据，不控制 Agent 自己的聊天历史、插件、浏览器缓存或概览用量统计。正文大小随请求量和上下文长度增长；保存一天不代表有固定容量上限。SQLite 删除记录后可能保留空闲页供后续复用。

配置字段为 `observability.retentionDays`，范围 1～365。写入服务每分钟读取一次保存设置；已删除的记录不能通过延长期限恢复。
