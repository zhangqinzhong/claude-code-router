# Weekly issue verification (2026-09-09)

Scope: issues created since 2026-09-07 00:00 Asia/Shanghai, on base
`b0d59afe` (`dev/3.1`). GitHub issue timelines and PRs updated since
2026-09-06 were checked. Only #1766 / PR #1767 and #1768 / PR #1769
had matching PRs at inspection time. Changes are local; no remote review,
merge, or push was submitted.

## #1768 - Claude App preferences lost on restore

- PR reviewed: https://github.com/musistudio/claude-code-router/pull/1769
  at `a259ddbcbf22ea2e62c8c2fd2185b74d1696f9bd`.
- Finding (P1): `restoreFileSnapshot(paths.metaFile, backup.metaFile)` still
  replaces the entire metadata file, discarding entries and preferences
  added during takeover. Root-key restoration also needs to avoid undoing
  a configuration switch performed by the user while CCR is running.
- Local solution follows the PR's library-preservation approach and restores
  only `deploymentMode` and `appliedId`, while CCR still owns their values.
- Regression cases: two apply/restore cycles, live library preferences,
  root preferences, newly added metadata entries, originally absent keys.
  Newly added regressions failed before the fix. Both Claude App gateway
  service and sync suites pass after the fix (12 tests, Electron Node runtime).

## #1772 - Deprecated Claude Desktop configuration keys

- No matching PR found.
- Removed the four reported unsupported keys from generated configuration
  and from existing entries during merge. Unknown user keys are preserved.
- Regression cases exercise clean configuration and migration in both the
  launch and active `-3p` directories. Two cases failed before the fix;
  all 12 service/sync tests pass after it. No live Desktop version matrix
  was available; validation covers emitted configuration and migration.

## #1775 - Repeated provider metadata catalog scans

- No matching PR found. Added a 512-entry cache keyed by all four request
  identity fields; identical requests and negative results reuse resolution.
- Deterministic regression instruments URL construction rather than asserting
  machine-dependent latency. Ten repeated lookup passes over 46 providers
  created 156,860 additional URLs before the fix and zero after warm-up.
- Tests also cover every identity field changing (including a mutated
  providerIds array), misses, and eviction. All six catalog tests pass.

## #1777 - Provider identity recomputation

- No matching PR found. WeakMap entries reuse each provider's alias set;
  name, explicit ID, provider family and base URL changes invalidate it.
  Enabled state and first-match ordering are still evaluated on each lookup.
- The 46-provider regression reduces identity hashes from 9,246 to 46
  across repeated misses and last-provider hits. All 24 identity-cache and
  routing architecture tests pass, including configuration mutation cases.

## #1776 - Slow app-info discovery

- No matching PR found. Inspection found eager synchronous PowerShell and
  repeated `where.exe` discovery before checking ordinary Windows install
  paths. Candidate enumeration is now lazy, including OpenCode's platform
  fallback, so a direct match avoids those subprocesses.
- Both web and Electron app-info handlers cache discovered paths (including
  absence) for 30 seconds. Actual launch discovery remains fresh.
- Tests cover Windows discovery with zero subprocess calls on direct hits,
  cache expiry, absent apps, retries and caller mutation. All 26 targeted
  discovery/catalog/profile tests and TypeScript checking pass.
- The reporter's Windows machine was unavailable. Cold discovery of absent
  apps can still require platform probes; no measured 7.2-second reduction
  on that machine is claimed.

## #1765 - Manual protocol selection does not persist

- No matching PR found. Saving a provider merged all old capabilities back
  into the current selection, including unchecked Anthropic Messages.
- Retain secondary endpoints only for selected chat protocols and preserve
  separately configured media capabilities. Removed chat protocols stay removed.
- Save/reopen regressions cover auto and manual modes on a custom corporate
  endpoint and failed before the fix. All 52 provider UI integration tests pass.
  The private corporate endpoint was not accessible for live verification.

## #1778 - Gemini protocol override and API key authentication

- No matching PR found. Exempted Google's `/v1beta/openai` endpoint from the
  hostname protocol lock which replaced OpenAI capabilities with native Gemini.
  Inference recognizes the compatibility endpoint; native-root constraints remain.
- Clicking a protocol checkbox selects manual mode. The #1765 save fix
  ensures deselected protocols also stay absent on reopen.
- AI Studio keys are no longer sent as OAuth bearer tokens by Gemini probes.
  The final native-Gemini provider hook uses the configured API key in the
  query/header and removes the invalid bearer form. OpenAI compatibility
  still uses bearer authentication.
- Both protocol regressions failed before the fix. Compilation, request
  routing, native/proxy auth boundaries, probe and provider UI suites pass
  (93 tests). Tests use stub upstreams; no paid Google call was made.

## #1780 - Required opencode-go session header

- No matching PR found. This issue reports CCR v2; this change targets the
  current v3 branch, whose provider runtime already supports `extraHeaders`.
- Configuration now also accepts `headers` and `extra_headers` aliases, with
  `extraHeaders` taking precedence. Example provider field:
  `"extraHeaders": { "x-opencode-session": "my-session" }`.
- Official HTTPS `opencode.ai/zen/go/v1` requests get a session header when
  missing. Explicit provider/client session headers are preserved; unrelated
  endpoints are untouched. Request IDs provide stable values across retries.
- Both regressions failed before the fix. All 10 header/configuration tests
  pass. No v2 release backport or live subscription request was performed.

## #1766 - Cowork plain WebSearch declaration

- PR reviewed: https://github.com/musistudio/claude-code-router/pull/1767
  at `460fae5506e12d67ab8f8ef582578626c9d2ed7e`.
- Finding (P1): the patch changes only Fusion configuration normalization,
  not inbound declarations. A plain Cowork `WebSearch` still fails
  `isAnthropicHostedWebSearchTool`, so hosted search is not activated.
  Preserving that spelling in configuration also disagrees with the bundled
  gateway's native tool-name matcher. The PR alone is insufficient.
- Local correction recognizes the exact reserved, untyped `WebSearch` name
  for selected Fusion search profiles, keeping gateway-compatible internal
  names. Evidence injection removes the intercepted tool and its forced
  tool choice while preserving unrelated custom search functions.
- Two request-level regressions failed before the fix. All targeted hosted
  search tests pass. No remote review or Cowork UI session was submitted/run.

## #1779 - Claude Code default auth and OpenRouter compatibility

- No matching PR found. Auto mode now uses the existing `apiKeyHelper`
  integration; a version string alone no longer enables federation. Explicit
  WIF opt-in is retained. No speculative undocumented SDK config is written.
- Direct OpenRouter records override merged cross-provider context/output
  maxima for exact model IDs. Claude discovery uses these defaults when no
  explicit provider metadata exists. Explicit lower windows override 1M flags.
- OpenRouter's official Chat capability is preferred for Anthropic clients
  when available, avoiding its incompatible Messages route for non-Anthropic
  models. Explicit single-protocol configurations and other hosts are preserved.
- Four regressions failed before fixes. The isolated full core run passes
  991 tests with 5 skips, including executing the generated helper and checking
  saved settings on a simulated Claude Code 2.1.235 installation.
- The bundled catalog has no exact `minimax/minimax-m3:free` record. Its window
  is not guessed from the paid model; live/configured 1M metadata is tested and
  honored. No reporter account or live CLI authentication was available.

## #1781 - Meta token minimum and tool-call IDs

- No matching PR found. Positive integer output limits below 16 are raised
  to 16 only for Muse Spark models on official OpenRouter Messages, Chat and
  Responses endpoints. Other models, hosts, encodings, omitted limits and
  invalid negative values are unchanged. Caller payloads are not mutated.
- The token-floor regression failed before the fix. Unit coverage checks
  all three token-limit fields and the 1/15/16/128 boundaries.
- A new integration test starts the installed ai-gateway 1.0.20 process,
  loads the actual CCR boundary plugin and redirects transport to a local
  stub. Twelve parallel tool calls/results retain every ID through both
  Anthropic -> Chat and Anthropic -> Responses, including the reported
  input-array position range. Both outbound requests carry the 16-token floor.
- **Unresolved:** the reported missing `call_id` has not been reproduced.
  Current adapters explicitly map `tool_use.id` and `tool_result.tool_use_id`
  to `call_id`; no fabricated IDs or discarded history have been introduced.
  A sanitized original request/full trace was requested to distinguish a
  payload edge case from OpenRouter's downstream Meta conversion. The #1779
  Chat preference avoids Messages when Chat is available, but this is not
  evidence that the reporter's exact failing request is fixed.
- OpenRouter currently documents a Messages endpoint, so the #1779 report
  is not treated as proof that all Messages calls are unsupported:
  https://openrouter.ai/docs/api/api-reference/anthropic-messages/create-messages

## Final verification

- `npm test`: 1,250 passed, 5 skipped, 0 failed. Breakdown: CLI 5;
  core 994 passed / 5 skipped; Electron 38; UI 208; architecture 5.
- `npm run typecheck`: passed.
- `npm run build:assets`: passed. The optional Claude documentation refresh
  could not access the network and reused existing metadata.
- `git diff --check`: passed.
- No paid provider calls, remote pushes, issue closures or PR review
  submissions were made. Desktop/Windows live behavior and #1781's exact
  failing payload remain outside the completed local verification.
